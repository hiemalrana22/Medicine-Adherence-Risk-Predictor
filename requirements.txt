# ============================================================
# Medication Adherence Predictor — LIVE DEMO dependencies
# ============================================================
# These are the ONLY packages the deployed Streamlit demo (app.py)
# needs. Kept deliberately slim and wheel-only so it deploys fast and
# never tries to compile anything from source on Streamlit Cloud / HF
# Spaces (that is what broke an earlier deploy: a pinned Pillow with no
# wheel for the runner's Python).
#
# For the full ML pipeline, R, tests, and notebooks, use:
#     pip install -r requirements-dev.txt
# ============================================================

streamlit>=1.35
pandas>=2.0
numpy>=1.26
plotly>=5.18
scikit-learn>=1.5
